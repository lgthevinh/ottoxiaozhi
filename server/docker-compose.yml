services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      - PYTHONUNBUFFERED=1
    # Local Postgres is disabled because this project uses Neon.
    # depends_on:
    #   - db
  # db:
  #   image: postgres:16-alpine
  #   environment:
  #     - POSTGRES_DB=ottoxiaozhi
  #     - POSTGRES_USER=ottoxiaozhi
  #     - POSTGRES_PASSWORD=ottoxiaozhi
  #   ports:
  #     - "5432:5432"
  #   volumes:
  #     - postgres_data:/var/lib/postgresql/data
  #     - ./db/schemas.sql:/docker-entrypoint-initdb.d/001_schemas.sql:ro

# volumes:
#   postgres_data:
